/**
 * whenever we load data from files, files may not contain the full data
 * so check the files data and start loading the pending data from a specfic page
 * of the close pull requests
 */
export const loadAndUpdateData = () => {
  // for later usage
};
